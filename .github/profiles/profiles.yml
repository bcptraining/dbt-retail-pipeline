dbt_retail_pipeline:
  target: dev
  outputs:
    dev:
      type: postgres
      host: localhost
      port: 5441
      user: dbt_user
      password: "{{ env_var('DEV_DB_PASSWORD') }}"
      dbname: retail_dev
      schema: "{{ env_var('DBT_USER') }}"
      threads: 4
