# Copy this file to .env and fill in your passwords.
# .env is gitignored and must never be committed.

DEV_DB_PASSWORD=changeme_dev
QA_DB_PASSWORD=changeme_qa
PROD_DB_PASSWORD=changeme_prod

# Your personal schema prefix for the dev environment (e.g. coryp_staging, coryp_facts)
DBT_USER=yourname
